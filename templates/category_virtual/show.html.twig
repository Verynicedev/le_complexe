{% extends 'base.html.twig' %}

{% block title %}CategoryVirtual{% endblock %}

{% block body %}
    <h1>CategoryVirtual</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category_virtual.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ category_virtual.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('category_virtual_index') }}">back to list</a>

    <a href="{{ path('category_virtual_edit', {'id': category_virtual.id}) }}">edit</a>

    {{ include('category_virtual/_delete_form.html.twig') }}
{% endblock %}
