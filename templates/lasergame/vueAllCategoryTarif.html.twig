{% extends 'lasergame/base.html.twig' %}

{% block title %} - Le Complexe{% endblock %}

{% block body %}
<main>
    <div class="header-laser d-flex align-items-center justify-content-center">
        <div class=" mx-auto w-100 bg-title-laser text-center">
            <h1 class="lasertitle text-white p-3">Laser Game Arena</h1>
            <h3 class="lasersubtitle text-danger"></h3>
            <p class="lasersubtitle h3 text-white p-3 font-weight-light">En famille ou entre amis</p>
        </div>
    </div>

    <div class="body-laser text bg-primary  container-fluid">
        <div class="bg-primary py-5">
            <h2 class="card-header bibo3 sdw my-3 text-center text-white">Choisir une catégorie</h2>
            <div class="row">
                {% for category in categories %}
                <div class="col-4 text-center mt-5 mx-auto divcatlaser">
                    <div class="card pt-5 mb-3 bg-secondary catlaser">
                        <h3 class="card-header bibo3 sdw text-light">{{ category.nom }}</h3>
                        <div class="card-body d-flex flex-column-reverse col-5 mx-auto">
                            <a href="{{ url('categorytarif',{id:category.id}) }}" class="card-link btn bibo3 sdw rounded text-light">Choisir</a>
                        </div>
                    </div>
                </div>
                {% endfor %}
            </div>
                <div class="row pt-5">
        <div class="">
            <a class="card-link btn bibo3 sdw rounded text-light" href="{{ path('lasergame') }}">Retour</a>
        </div>
    </div>
        </div>
    </div>
        {# {% for category in categories %}
        <p><a href="{{ url('categorytarif',{id:category.id}) }}" class="text-white text-center">{{ category.nom }}</a></p>
        <img src="{{ asset('img/cityfarWest.jpg') }}" alt="lasergame" class=" img-fluid rounded border border-primary">
        {% endfor %} #}
</main>
{% endblock %}