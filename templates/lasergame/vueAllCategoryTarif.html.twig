{% extends 'base.html.twig' %}

{% block title %} - Le Complexe{% endblock %}

{% block body %}
<main>
    <div class="mt-5">
        <h3 class="text-white text-center">Catégories de nos tarifs Laser game</h3>
        <div class="text-center py-3 articles-about">
            {% for tarif in tarifs %}
            <a href="{{ url('categoryVR',{id:tarif.category.id}) }}" class="text-white">{{ tarif.category.nom }}</a>
            {% endfor %}
        </div>   
    </div>
    
</main>
{% endblock %}