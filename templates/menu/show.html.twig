{% extends 'category_tarif/base.html.twig' %}

{% block title %}Menu{% endblock %}

{% block body %}
    {# <h1>Menu</h1> #}
<div class="padt">
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ menu.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ menu.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ menu.description }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ menu.image }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ menu.prix }}</td>
            </tr>
        </tbody>
    </table>
</div>
        <div class="pl-5">
        <div class="pb-2 w-75">
            <a href="{{ path('menu_edit', {'id': menu.id}) }}" class="btn btn-primary">Modifer</a>
        </div>
        <div>
            {{ include('menu/_delete_form.html.twig') }}
        </div>
    </div>
    <hr>
    <div class="p-3">
        <a href="{{ path('menu_index') }}" class="btn btn-primary">Retour</a>
    </div>

{% endblock %}
