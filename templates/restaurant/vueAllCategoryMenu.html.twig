{% extends 'base.html.twig' %}

{% block title %} Les menus du restaurant- Le Complexe{% endblock %}

{% block body %}
<main>
    <div class="mt-5">
        <h3 class="text-white text-center">Choisis ton repas</h3>
        <div class="text-center py-3 articles-about">
            {% for category in categories %}
            <a href="{{ url('categoryMenu',{id:category.id}) }}" class="text-white">{{ category.nom }}</a>
            {% endfor %}
        </div>   
    </div>
    
</main>
{% endblock %}